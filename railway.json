{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python -c 'import sys; print(\"=== Python OK ===\"); sys.path.insert(0, \"/app\"); from app import main; print(\"=== Import OK ===\")' && alembic upgrade head && uvicorn app.main:app --host 0.0.0.0 --port $PORT --log-level debug",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
